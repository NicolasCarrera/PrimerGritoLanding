---
import { Picture } from "astro:assets";
import book from "../../../assets/images/book-cover-placeholder.png";
import BuyButton from "../../../components/BuyButton.astro";
---

<section id="hero" class="hero">
  <div class="hero-container">
    <div class="hero-left">
      <Picture src={book} alt="Portada del libro Primer Grito" format="webp" />
    </div>
    <div class="hero-right">
      <h1>Primer Grito</h1>
      <p>
        Primer Grito es una novela histórica ecuatoriana que retrata el camino
        hacia la independencia del Ecuador desde una mirada íntima, humana y
        profundamente andina. Entre 1809 y 1822, en una tierra marcada por la
        dominación colonial y el despertar de una nueva conciencia política, dos
        jóvenes de mundos opuestos quedan unidos por un amor imposible.
      </p>
      <p>
        Isabel María Altamirano de la Cruz, hija de una influyente familia
        criolla de Quito, crece atrapada entre los deberes de su clase social y
        la vigilancia constante del poder español. José, un joven indígena de
        Latacunga, carga con el peso de una historia ancestral y una lucidez
        temprana frente a la injusticia del orden colonial.
      </p>
      <p>
        Desde el Primer Grito de Independencia hasta la Batalla de Pichincha, la
        novela entreteje hechos históricos reales con la intensidad de un
        romance prohibido que los arrastra —inevitablemente— a formar parte de
        una revolución destinada a cambiar sus vidas para siempre.
      </p>
      <BuyButton text="Comprar Ahora" />
    </div>
  </div>
</section>

<style>
  .hero {
    background-color: var(--color-blue-dark);
    background-image: var(--background-hero);
    color: var(--text-color-dark-bg);
    display: flex;
    align-items: center;
    width: 100%;
    overflow: hidden;
  }

  .hero-container {
    display: flex;
    flex-direction: column-reverse;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-unit);
    padding-bottom: var(--spacing-unit);
    width: 100%;
    max-width: 100%;
  }

  .hero-left {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    max-width: 100%;
    overflow: hidden;
  }

  .hero-left img {
    width: 100%;
    max-width: 280px;
    height: auto;
    object-fit: contain;
    filter: drop-shadow(0 20px 40px rgba(0, 0, 0, 0.5));
  }

  .hero-right {
    flex: 1;
    text-align: center;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: var(--spacing-unit);
    width: 100%;
    max-width: 100%;
    box-sizing: border-box;
    gap: 1.5rem;
  }

  .hero-right h1 {
    font-size: clamp(2rem, 4vw, 3.5rem);
    margin: 0;
    padding: 0;
    font-weight: 400;
    letter-spacing: 2px;
    color: var(--color-gold);
    font-family: var(--font-family-heading);
    word-wrap: break-word;
    overflow-wrap: break-word;
    line-height: 1.2;
  }

  .hero-right p {
    margin: 0;
    padding: 0;
    font-size: clamp(0.9rem, 1.2vw, 1rem);
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.95);
    word-wrap: break-word;
    overflow-wrap: break-word;
    max-width: 100%;
  }

  .hero-right .btn {
    margin: 0;
    padding: 0.75rem 2rem;
    width: auto;
    max-width: 100%;
    align-self: center;
  }

  @media (min-width: 768px) {
    .hero-left img {
      max-width: 320px;
    }

    .hero-right {
      gap: 2rem;
    }
  }

  @media (min-width: 1024px) {
    .hero {
      height: 100vh;
      min-height: 600px;
    }

    .hero-container {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      gap: 2rem;
    }

    .hero-left {
      width: 40%;
      flex: 0 1 40%;
      max-width: 40%;
    }

    .hero-left img {
      max-width: 380px;
    }

    .hero-right {
      width: 60%;
      flex: 0 1 60%;
      max-width: 60%;
      text-align: left;
      align-items: flex-start;
      padding: 3rem;
      gap: 2.5rem;
    }

    .hero-right .btn {
      align-self: flex-start;
    }
  }
</style>
